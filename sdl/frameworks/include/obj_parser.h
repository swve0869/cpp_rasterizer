#pragma once

#include <stdio.h>;
#include <raster.h>;


//object3d parse_obj_file(std::string filename) {
   /* std::istream in = std::ifstream(filename, std::ios::in);

    if (!in)
    {
        std::cerr << "Cannot open " << filename << std::endl;
        exit(1);

    }*/
    
//}
